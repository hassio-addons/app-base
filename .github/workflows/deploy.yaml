---
name: Deploy

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_run: # zizmor: ignore[dangerous-triggers]
    workflows: ["CI"]
    branches: [main]
    types:
      - completed

permissions:
  contents: read
  packages: write

jobs:
  workflows:
    uses: hassio-addons/workflows/.github/workflows/base-deploy.yaml@19abc57e997a6e14cb4ba35968a442e1fadbfbc2 # main
