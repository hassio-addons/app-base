---
name: Deploy

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
  release:
    types:
      - published

permissions:
  actions: read
  contents: read
  packages: write
  security-events: write

jobs:
  ci:
    uses: hassio-addons/workflows/.github/workflows/base-ci.yaml@bf24a85062d78a850e6747e4040527d40e023abd # v2.0.5

  deploy:
    needs: ci
    uses: hassio-addons/workflows/.github/workflows/base-deploy.yaml@bf24a85062d78a850e6747e4040527d40e023abd # v2.0.5
